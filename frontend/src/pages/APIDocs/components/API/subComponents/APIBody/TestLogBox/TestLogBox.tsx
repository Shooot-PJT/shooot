import { roundedBorder } from './TestLogBox.css';

export const TestLogBox = () => {
  return (
    <div
      className={roundedBorder}
      style={{
        height: '5rem',
      }}
    ></div>
  );
};
